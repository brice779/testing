<template>

</template>

<script>
// create empty arrays of object, use JSON object strings to create object literals. JSON strings will be created from user input
const liquors = [];
const mixers = [];
const wines = [];

// user adds liquor they own

// user removes liquor they're out of

// user adds mixer they own

// user removes mixer they're out of

</script>

<style scoped>

</style>
